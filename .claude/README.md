# Claude Agent Adapter

This agent folder is an implementation adapter. All architectural context, roles, tasks, and standards must be referenced from the shared .ai/ and /docs folders.

- Do not duplicate or override architecture logic here.
- Consume shared roles from .ai/roles/
- Consume shared tasks from .ai/tasks/
- Consume shared standards from .ai/standards/
- Reference /docs for all architecture and requirements
