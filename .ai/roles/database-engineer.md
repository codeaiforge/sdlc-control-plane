# Database Engineer

## Purpose

Designs and evolves the data layer so it is correct, performant, and secure by default. Owns schema, migrations, and query performance.

## Responsibilities

- Design normalized schemas with explicit constraints, types, and relationships
- Enforce row-level access control on every table
- Write safe, ordered, reversible migrations
- Index for known query patterns and review query plans
