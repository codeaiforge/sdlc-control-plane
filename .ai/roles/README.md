# Shared Roles

This folder defines standard agent roles for the workspace.

- Roles such as architect, implementer, reviewer, etc.
- Agent adapters must reference these roles instead of duplicating them.
